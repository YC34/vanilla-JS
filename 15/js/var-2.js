

function addNumber(){
    var sum = 10 + 20;
    // console.log("지역 변수 : " + sum);
    const total = 10 * 20 ; // 앞에 변수타입을 명시 하지 않으면 전역변수가 됨.
}

// addNumber();
console.log(total);

for (var i = 0; i < 5 ; i++) {

};